# OS X fullscreen mode that is compatible with Workspaces.
fullscreen = non-native-visible-menu
# I'm old school, don't touch my `!=`
font-feature = -calt
# <cmd+`> to toggle quake terminal
#keybind = global:cmd+backquote=toggle_quick_terminal
