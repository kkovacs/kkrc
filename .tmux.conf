unbind C-b
set -g prefix c-a
bind a send-prefix
bind c-a last-window

# Switch windows
bind BSpace previous-window
bind Space next-window
bind -n C-Space next-window
# Jump to window with bell
bind c-x next-window -a

# Reload tmux config
bind R source-file ~/.tmux.conf \; display '.tmux.conf reloaded'

# Toggle broadcasting input to all panes.
bind b set synchronize-panes

# Toggle tmux "mouse" functionality
bind m set mouse
bind M set mouse on

# Copy mode
bind Escape copy-mode

# Paste immediately
bind c-p paste-buffer
bind p choose-buffer -Z
bind P choose-buffer

# Keys INSIDE copy-mode
bind -T copy-mode-vi Escape send -X cancel
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi V send -X select-line
bind -T copy-mode-vi C-v send -X rectangle-toggle
bind -T copy-mode-vi Enter send -X copy-selection-and-cancel
bind -T copy-mode-vi y send -X copy-selection-and-cancel
bind -T copy-mode-vi Y send -X append-selection-and-cancel

# Pane management
bind Enter select-pane -m
bind S join-pane
bind V join-pane -h
bind o break-pane
bind O break-pane -d

# Various comfort settings stolen from all over the net
set -g status-keys vi
set -g renumber-windows on
set -g history-limit 100000
set -g mode-keys vi
set -g mouse on
set -g automatic-rename on
set -g set-titles on
set -sg escape-time 0
set -g display-time 1500
# Differs from main:
set -g detach-on-destroy on

# Split
bind v split-window -h
bind "|" split-window -h
bind s split-window -v

# New session
bind C command-prompt "new-session -s '%%'"

# Window chooser
bind "\"" choose-window -Z
bind "'" choose-session -Z

# Rename window
bind A command-prompt -I "#W" "rename-window '%%'"

# Move window left and right
bind , swap-window -d -t -1
bind . swap-window -d -t +1

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind c-h select-pane -L
bind c-j select-pane -D
bind c-k select-pane -U
bind c-l select-pane -R
bind [ select-pane -t :.-
bind ] select-pane -t :.+
bind Tab select-pane -t :.+

# Toggle monitoring activity and/or silence. Have the same keys as `screen`
bind "#" set -w monitor-activity
bind _ if -F '#{monitor-silence}' 'set -w monitor-silence 0' 'set -w monitor-silence 30'
set -g activity-action any
# Default any: set -g bell-action any

# Clear pane
bind r send-keys -R \; clear-history \; refresh-client \; send-keys ^L

# Kill pane/window
bind x confirm-before -p "kill-pane #P? (y/n)" kill-pane

# GNU-screen status simulator
set -g status on
set -g status-position bottom
set -g status-style bg=gray,fg=black
set -g message-style bg=gray,fg=black

# Window list format (non-current)
set -g window-status-format " #I:#W#F "
set -g window-status-separator ""
set -g window-status-style bg=gray,fg=black

# Current window
set -g window-status-current-format " #I:#W#F "
set -g window-status-current-style bg=blue,fg=white,bold

# Activity style
set -g window-status-activity-style bg=black,fg=white
set -g window-status-bell-style bg=black,fg=white

# No left side content
set -g status-left ""
set -g status-left-style bg=gray,fg=white

# Right side: Small non-intrusive indicators. Hostname in white-on-blue.
set -g status-right "#[bg=gray,fg=black] #{?pane_pipe,P,}#{?window_zoomed_flag,Z,}#{?monitor-activity,a,}#{?monitor-silence,s,}#{?mouse,,M}#{?synchronize-panes,B,}#{?session_many_attached,M,} #[default] #H #[bg=gray,fg=black] %H:%M %Z "
set -g status-right-style bg=blue,fg=white

# Leave these last so they don't stop config loading - incompatible with tmux on Ubuntu 24.04 and before :(
set -gs extended-keys on
set -gs -q extended-keys-format csi-u
