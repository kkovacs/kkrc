SESH=$(ls -t ~/.pi/agent/sessions/--"$(echo "${PWD#/}" | tr '/' '-')"--/*.jsonl 2>/dev/null | head -1); jq -r 'select(.type=="message") | if .message.role == "user" then "user: " + ([.message.content[]|select(.type=="text")|.text]|join("")) elif .message.role == "assistant" then "assistant: " + ([.message.content[]|select(.type=="text")|.text]|join("")) elif .message.role == "bashExecution" then "! " + .message.command else empty end' "$SESH"
