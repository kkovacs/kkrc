!SESH=$(ls -t ~/.pi/agent/sessions/--"$(echo "${PWD#/}"|tr '/' '-')"--/*.jsonl 2>/dev/null|head -1) ; jq -r 'select(.type=="message")|if .message.role=="user"then"# User","",([.message.content[]|select(.type=="text")|.text]|join("")),""elif .message.role=="assistant"then([.message.content[]|select(.type=="text"or(.type=="toolCall"and(.name=="read"or .name=="write"or .name=="edit")))])as$c|if($c|length)>0then"# Assistant","",($c[]|if.type=="text"then.text elif.type=="toolCall"then"["+.name+"] "+.arguments.path else empty end),""else empty end elif .message.role=="bashExecution"then"[bash] $ "+.message.command,(if .message.fullOutputPath then"[output: "+.message.fullOutputPath+"]"else .message.output,(if .message.truncated then"[output truncated]"else empty end)end),""else empty end' "$SESH"
