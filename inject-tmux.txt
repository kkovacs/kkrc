i tmux unbind C-b \; set -g prefix c-a \; bind a send-prefix \; bind c-a last-window \; bind BSpace previous-window \; bind Space next-window \; bind -n C-Space next-window \; bind c-x next-window -a \; bind R source-file ~/.tmux.conf \\\; display '.tmux.conf reloaded' \; bind b set synchronize-panes \; bind m set mouse \; bind M set mouse on \; bind Escape copy-mode \; bind c-p paste-buffer \; bind p choose-buffer -Z \; bind P choose-buffer \; bind -T copy-mode-vi Escape send -X cancel \; bind -T copy-mode-vi v send -X begin-selection \; bind -T copy-mode-vi V send -X select-line \; bind -T copy-mode-vi C-v send -X rectangle-toggle \; bind -T copy-mode-vi Enter send -X copy-selection-and-cancel \; bind -T copy-mode-vi y send -X copy-selection-and-cancel \; bind -T copy-mode-vi Y send -X append-selection-and-cancel \; bind Enter select-pane -m \; bind S join-pane \; bind V join-pane -h \; bind o break-pane \; bind O break-pane -d \; set -g status-keys vi \; set -g renumber-windows on \; set -g history-limit 100000 \; set -g mode-keys vi \; set -g mouse on \; set -g automatic-rename on \; set -g set-titles on \; set -sg escape-time 0 \; set -g display-time 1500 \; set -g detach-on-destroy on \; bind v split-window -h \; bind "|" split-window -h \; bind s split-window -v \; bind C command-prompt "new-session -s '%%'" \; bind "\"" choose-window -Z \; bind "'" choose-session -Z \; bind A command-prompt -I "#W" "rename-window '%%'" \; bind , swap-window -d -t -1 \; bind . swap-window -d -t +1 \; bind h select-pane -L \; bind j select-pane -D \; bind k select-pane -U \; bind l select-pane -R \; bind c-h select-pane -L \; bind c-j select-pane -D \; bind c-k select-pane -U \; bind c-l select-pane -R \; bind [ select-pane -t :.- \; bind ] select-pane -t :.+ \; bind Tab select-pane -t :.+ \; bind "#" set -w monitor-activity \; bind _ if -F '#{monitor-silence}' 'set -w monitor-silence 0' 'set -w monitor-silence 30' \; set -g activity-action any \; bind r send-keys -R \\\; clear-history \\\; refresh-client \\\; send-keys ^L \; bind x confirm-before -p "kill-pane #P? (y/n)" kill-pane \; set -g status on \; set -g status-position bottom \; set -g status-style bg=gray,fg=black \; set -g message-style bg=gray,fg=black \; set -g window-status-format " #I:#W#F " \; set -g window-status-separator "" \; set -g window-status-style bg=gray,fg=black \; set -g window-status-current-format " #I:#W#F " \; set -g window-status-current-style bg=blue,fg=white,bold \; set -g window-status-activity-style bg=black,fg=white \; set -g window-status-bell-style bg=black,fg=white \; set -g status-left "" \; set -g status-left-style bg=gray,fg=white \; set -g status-right " #H #[bg=gray,fg=black] %H:%M %Z " \; set -g status-right-style bg=blue,fg=white \; set -gs extended-keys on \; set -gs -q extended-keys-format csi-u
